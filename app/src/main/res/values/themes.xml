<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyNotes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/blue</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="Theme.MyNotes" parent="Base.Theme.MyNotes"></style>

    <style name="AlertDialogStyle"
        android:parent="ThemeOverlay.MaterialComponents.Dialog.Alert"
        xmlns:android="http://schemas.android.com/apk/res/android">
        <item name="android:background">@color/bgColorSecondary</item>
        <item name="android:textColor">@color/textColorSecondary</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
    </style>
</resources>